meta:
  environment: docker
  toolchain:
    name: docker
    version: latest

build:
  dockerfile: Dockerfile
  skip: false

run:
  containerPort: 3000
  servicePort: 80
  persistenceMount: /app/data
  singleton: true
  zeroDowntime: false